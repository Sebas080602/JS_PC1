// Convertir a numero
let cadena = "123";
let numero = Number(cadena);
console.log(numero); // 123 (tipo número)

// Convertir a String:
let numero1 = 456;
let cadena1 = String(numero1);
console.log(cadena1); // "456" (tipo string)

// Convertir a Booleano
let valor = 0;
let booleano = Boolean(valor);
console.log(booleano); // false



